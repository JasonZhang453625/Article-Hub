/// Width thresholds for responsive layout. All values in logical pixels
/// (dp on Android, vp on HarmonyOS).
///
/// * [tabletBreakpoint] — switches from bottom navigation to side rail.
/// * [desktopBreakpoint] — switches from single-pane to master-detail layout.
const double tabletBreakpoint = 600;
const double desktopBreakpoint = 960;
